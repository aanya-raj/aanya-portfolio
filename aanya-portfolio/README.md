# frontend

react + vite + typescript + tailwind + framer motion.

```bash
npm install
npm run dev
```

`npm run build` for production.

the fun stuff lives in `src/components` (tarot cards, the card reader,
sparkles, an unreasonable amount of framer motion). the words and
numbers live in `src/data`.
